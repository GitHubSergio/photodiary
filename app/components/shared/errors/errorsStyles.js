// Imports
import { StyleSheet } from 'react-native';
import Colors from '../../../constants/Colors';

const errorsStyles = StyleSheet.create({
  errorView: {
    padding: 10,
  },
  text: {
    color: Colors.redMain,
  },
});

export default errorsStyles;
