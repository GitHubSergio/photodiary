// Imports
import { StyleSheet } from 'react-native';
import Colors from '../../../constants/Colors';

const noPostsStyles = StyleSheet.create({
  noPostsView: {
    flex: 1,
    justifyContent: 'center',
  },
  text: {
    fontSize: 50,
    color: Colors.bermudaGrey,
    textAlign: 'center',
  },
});

export default noPostsStyles;
